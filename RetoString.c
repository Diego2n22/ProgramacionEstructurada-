#include <stdio.h>
#include <stdlib.h>

int main()
{
     char frase[50];
    int size;

    printf("Invertir la frase. \n");

    printf("Ingrese la frase: ");
    gets(frase);
    printf("Leer frase");
    puts(frase);

    return 0;
}
